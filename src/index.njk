---
title: 
layout: main
eleventyExcludeFromCollections: true
---
<h1>{{ site.title }}</h1>
<h2>🎄Welcome to my Digital Garden!🎄</h2>
Recently updated:
<ul>
{%- for note in collections.all | reverse -%}
  <li>
    <a href="{{ note.data.permalink }}">{{ note.data.title }}</a> - {{ note.date.toUTCString() }}
  </li>
{%- endfor -%}
</ul>
Popular tags:
<ul>
{% for tag in collections.tagsList | tagsSortByCountDesc %}
  <li>
    <a href="/tags/{{ tag.name }}">{{ tag.name }} ({{ tag.count }})</a>
  </li>
{% endfor %}
</ul>
